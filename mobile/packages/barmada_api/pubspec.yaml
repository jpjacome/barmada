name: barmada_api
description: Typed Dart client for the Barmada /api/v1 staff API.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  dio: ^5.7.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
